{
  "//": "ZeRO-2 config for the Day-2 2-GPU DPO training demo. Invoked via `accelerate launch --config_file configs/accelerate_zero2.yaml scripts/train_dpo.py --config configs/train_condition_c.yaml`; accelerate passes this file's path through to deepspeed. Also referenced by configs/train_condition_b.yaml and train_condition_c.yaml's deepspeed_config_path field for a direct `deepspeed scripts/train_dpo.py` launch.",
  "bf16": {
    "enabled": true
  },
  "zero_optimization": {
    "stage": 2,
    "overlap_comm": true,
    "contiguous_gradients": true,
    "reduce_bucket_size": 5e8,
    "allgather_bucket_size": 5e8
  },
  "gradient_accumulation_steps": "auto",
  "gradient_clipping": "auto",
  "train_batch_size": "auto",
  "train_micro_batch_size_per_gpu": "auto"
}
